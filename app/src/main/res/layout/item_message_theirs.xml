<?xml version="1.0" encoding="utf-8"?>
<!-- FILE PATH: app/src/main/res/layout/item_message_theirs.xml -->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="start"
    android:layout_marginBottom="8dp"
    android:layout_marginEnd="64dp">

    <!-- Avatar circle with initials -->
    <TextView
        android:id="@+id/tvSenderAvatar"
        android:layout_width="28dp"
        android:layout_height="28dp"
        android:layout_marginEnd="8dp"
        android:layout_marginTop="2dp"
        android:gravity="center"
        android:textSize="11sp"
        android:textColor="#57738F"
        android:textStyle="bold"
        android:background="@drawable/bg_chat_avatar_peer"
        android:text="?"/>

    <!-- Bubble -->
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:background="@drawable/bg_chat_message_theirs"
        android:paddingStart="14dp"
        android:paddingEnd="14dp"
        android:paddingTop="9dp"
        android:paddingBottom="8dp">

        <TextView
            android:id="@+id/tvMessageText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Message"
            android:textSize="14sp"
            android:textColor="#1B2A3A"
            android:lineSpacingExtra="2dp"
            android:maxWidth="250dp"/>

        <TextView
            android:id="@+id/tvMessageTime"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="end"
            android:text="12:00 PM"
            android:textSize="10sp"
            android:textColor="#73879B"
            android:layout_marginTop="4dp"/>
    </LinearLayout>

</LinearLayout>